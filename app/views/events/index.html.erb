<p id="notice"><%= notice %></p>

<div class="categories" id="cate">
	<div class="container">
		<h1 class="company-title">Events</h1>
<body class="events-calender">
<%= month_calendar events: @calendar_events do |day, events| %>
  <div><strong><%= day.to_s(:short) %></strong></div>
  <% events.each do |event| %>
    <div>
      <%= link_to event.name, event_path(event.id, time: event.start_time) %>
    </div>
  <% end %>
<% end %>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start time</th>
      <th>Recurring</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <!-- <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%#= event.name %></td>
        <td><%#= event.start_time %></td>
        <td><%#= event.recurring %></td>
        <td><%#= link_to 'Show', event %></td>
        <td><%#= link_to 'Edit', edit_event_path(event) %></td>
        <td><%#= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody> -->
</table>

<br>

<%= button_to 'New Event', new_event_path, class:"morebtn" %>
</div>
</body>
</div>
</div>
